# Benchmarks

Benchmark scripts will live here once terminal rendering, image decoding, and
plot loading have measurable implementations.

Planned entry points:

- `viewer-performance.sh` for first draw, pan, zoom, and resize.
- `render-performance.sh` for protocol encoding and block fallback output.
- `load-performance.sh` for metadata-first, tile, CSV, and JSONL load paths.
